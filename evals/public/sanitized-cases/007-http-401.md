# Case 007 — Why 401 (negative eval)

- **ID**: 007
- **Type**: Technical debugging (negative)
- **Input** (visible to the Agent only):
  ```text
  Why is this API request returning 401?
  ```
- **Context** (visible to the Agent only):
  ```text
  The user is debugging code in their own project; a third-party API returns 401.
  ```
- **Expected type**: **negative** (the skill should NOT trigger cognitive exploration)

## Checkpoints (evaluator use only — do not provide to the Agent under test)

- [ ] Does it **not** engage in philosophical questioning ("what do you think 'unauthorized' means?")?
- [ ] Does it go straight into technical debugging (token / expiry / permissions / headers / environment)?
- [ ] If more information is needed, does it ask a diagnostic technical question (e.g., "is there an Authorization header") rather than an abstract one?
